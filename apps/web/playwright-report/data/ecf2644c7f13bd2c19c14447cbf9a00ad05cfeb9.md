# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Clerk Authentication & Route Protection >> protected dashboard route redirects unauthenticated users to sign-in
- Location: e2e\auth.spec.ts:14:7

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('h1')
Expected pattern: /Sign in/i
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('h1')

```

```yaml
- main
- region "Notifications alt+T"
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Clerk Authentication & Route Protection', () => {
  4  |   test('public landing page is accessible when unauthenticated', async ({ page }) => {
  5  |     await page.goto('/');
  6  |     await expect(page).toHaveURL('/');
  7  |   });
  8  | 
  9  |   test('/sign-in page loads Clerk sign in interface', async ({ page }) => {
  10 |     await page.goto('/sign-in');
  11 |     await expect(page.locator('h1')).toContainText(/Sign in/i);
  12 |   });
  13 | 
  14 |   test('protected dashboard route redirects unauthenticated users to sign-in', async ({ page }) => {
  15 |     await page.goto('/dashboard');
  16 |     await expect(page).toHaveURL(/\/sign-in/);
> 17 |     await expect(page.locator('h1')).toContainText(/Sign in/i);
     |                                      ^ Error: expect(locator).toContainText(expected) failed
  18 |   });
  19 | });
  20 | 
```